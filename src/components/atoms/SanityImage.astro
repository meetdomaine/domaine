---
import { urlFor } from "../../utils/sanityClient";
const { image, width, height, alt } = Astro.props;

const url = urlFor(image).url();
---
<img
  src={url}
  width={width}
  height={height}
  decoding="async"
  loading="lazy"
  alt={alt}
/>