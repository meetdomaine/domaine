---
const { heading } = Astro.props
---
<section class="home_hero">
    <h1 data-pagefind-filter="type:page">{heading}</h1>
</section>

<style>
    .home_hero {
        padding-inline: var(--page-margin);
        min-height: 80svh;
        display: flex;
        flex-direction: column;
        justify-content: center
    }

    h1 {
        max-width: 8em;
    }
</style>