---
import AtomSanityMedia from "../atoms/atom-sanityMedia.astro"

interface Props {
    title: string,
    slug: any,
    thumbnailImage: any,
    category: any,
    agencyBrand: any
}

const { title, thumbnailImage, slug, category, agencyBrand } = Astro.props
---
<a href={`${agencyBrand.slug.current == 'studio' ? '/studio' : ''}/insights/${category.slug.current}/${slug.current}`} class="blog-card">
    <AtomSanityMedia
        image={thumbnailImage.image}
        aspectRatio={3 / 4}
        sizes="30vw"
    />
    <h3>{title}</h3>

</a>

<style>
    .blog-card {
        flex: 0 1 100%;
    }
</style>