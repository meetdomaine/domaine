---
import GlobalProjectCard from './global_ProjectCard.astro'

interface Props {
    projects: Array<ProjectCard>,
    brand: string
}

const { projects, brand } = Astro.props
// console.log(projects)
---
{projects.map((project) => (
    <GlobalProjectCard
        title={project.title}
        excerpt={project.excerpt}
        heroMedia={project.heroMedia}
        thumbnailMedia={project.thumbnailMedia}
        slug={project.slug}
        brand={brand}
    />
))}