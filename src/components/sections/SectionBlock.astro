---
import Hero from './hero/Hero.astro';
import Clients from './clients/Clients.astro';
import About from './about/About.astro';
import Services from './services/Services.astro';
import Agencies from './agencies/Agencies.astro';
import Partners from './partners/Partners.astro';
import Contact from './contact/Contact';
import Work from './work/Work';


interface Props {
	block: any;
}
const { block } = Astro.props;

---

<!-- {block._type == 'sectionHero' ? <Hero content={block} /> : null}
{block._type == 'sectionClients' ? <Clients content={block} /> : null}
{block._type == 'sectionAbout' ? <About content={block} /> : null}
{block._type == 'sectionServices' ? <Services content={block} /> : null}
{block._type == 'sectionContact' ? <Contact client:only="solid-js" content={block} /> : null}
{block._type == 'sectionWork' ? <Work client:idle content={block} /> : null} -->
{block._type == 'sectionPartners' ? <Partners content={block} /> : null}
{block._type == 'sectionAgencies' ? <Agencies content={block} /> : null}