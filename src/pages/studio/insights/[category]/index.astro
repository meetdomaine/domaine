---
import { sanityClient } from 'sanity:client';
import { blogCardFields } from '../../../../components/global/utils/cms-queries';
import LayoutStudioBlogIndex from '../../../../layouts/Layout-Studio-BlogIndex.astro';
import { allBlogCategories_Studio, allBlogPosts_Studio } from '../../../../components/global/utils/cms-content';

export async function getStaticPaths() {
    const categories = allBlogCategories_Studio
    return categories.map((category) => {
        // console.log(category)
        return {
            params: { category: category.slug.current },
            props: { content: category }
        }
    })
}

interface Props {
    content: any
}

const { content } = Astro.props;

const pageSettings = await sanityClient.fetch(`*[_type == "page_blog-index" && _id == "page_blog-index-studio"][0]`)
const blogPosts = allBlogPosts_Studio.filter((post) => post.category._id === content._id)
---
<LayoutStudioBlogIndex
    pageSetting={pageSettings}
    blogPosts={blogPosts}
    heading={content.title}
    subheading="Lorem ipsum dolor sit amet."
    searchFilter="type:blog-category"
    slug={content.slug}
>

</LayoutStudioBlogIndex>
