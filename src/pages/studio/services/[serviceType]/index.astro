---
import { getCollection } from "astro:content"
import LayoutStudioServiceType from "../../../../layouts/Layout-Studio_ServiceType.astro"

export async function getStaticPaths() {
    const serviceTypes = await getCollection('serviceTypes_Studio')
    return serviceTypes.map((serviceType) => {
        return {
            params: { serviceType: serviceType.id },
            props: { content: {...serviceType.data} }
        }
    })
}

const { content } = Astro.props;
---
<LayoutStudioServiceType 
    content={content}
/>