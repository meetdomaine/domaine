---
import { sanityClient } from 'sanity:client';
import LayoutDomaineBlogIndex from '../../../layouts/Layout-Domaine-BlogIndex.astro';
import { allBlogCategories_Domaine, allBlogPosts_Domaine } from '../../../components/global/utils/cms-content';

export const prerender = true

export async function getStaticPaths() {
    const categories = await allBlogCategories_Domaine
    return categories.map((category) => {
        // console.log(category)
        return {
            params: { category: category.slug.current },
            props: { content: category }
        }
    })
}

interface Props {
    content: any
}

const { content } = Astro.props;

const pageSettings = await sanityClient.fetch(`*[_type == "page_blog-index" && _id == "page_blog-index-domaine"][0]`)
const blogPosts = allBlogPosts_Domaine.filter((post) => post.category._id === content._id)
---
<LayoutDomaineBlogIndex
    pageSetting={pageSettings}
    blogPosts={blogPosts}
    title={`${content.title} | Insights`}
    heading={content.title}
    subheading={content.heading}
    searchFilter="type:blog-category"
    slug={content.slug}
>

</LayoutDomaineBlogIndex>
